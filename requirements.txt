fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
openai>=1.30

# Optional analytics backends (uncomment if you enable ANALYTICS_BACKEND)
# redis>=5.0          # for ANALYTICS_BACKEND=redis
# supabase>=2.4       # for ANALYTICS_BACKEND=supabase (or just use httpx REST directly)
